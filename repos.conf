# Repositories to clone into repos/.
# One repo name per line. Blank lines and comments (#) are ignored.
# Format: <name>  (cloned from $GITHUB_ORG/<name>)

roslyn
deepstaging
deepstaging-web
assets
.github
